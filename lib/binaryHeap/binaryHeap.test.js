const should = require('should');
const algo = require('../index.js');

describe('test binaryHeap', () => {
    let binaryHeap = [];

    beforeEach(() => {
        binaryHeap = new algo.binaryHeap();
    });

   describe('Binary heap', () => {
	it('Should add el to heap', () => {
      
	});
});
});