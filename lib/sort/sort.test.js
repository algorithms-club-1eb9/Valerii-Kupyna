const should = require('should');
const search = require('../index.js');

describe('Sort', () => {

    it('shoud be [1, 2, 3, 4, 5]]', () => {
   
        should().be.eql([1, 2, 3, 4, 5]);
    });

});